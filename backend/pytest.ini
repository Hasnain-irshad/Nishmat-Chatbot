[pytest]
testpaths = tests
pythonpath = .
addopts = -q --strict-markers
asyncio_default_fixture_loop_scope = function
asyncio_mode = auto
markers =
    live: touches the real Supabase project or a running API
